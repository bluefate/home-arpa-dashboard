# Local image build from a git clone (optional).
#   docker compose -f docker-compose.yml -f docker-compose.build.yml up -d --build

services:
  arpa-dashboard:
    build: .
    image: ghcr.io/bluefate/home-arpa-dashboard:local
